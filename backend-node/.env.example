# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Settings
ALLOWED_ORIGINS=https://dcktoken.com,https://www.dcktoken.com,http://localhost:5173

# Solana RPC
RPC_HTTP=https://api.mainnet-beta.solana.com
RPC_WSS=wss://api.mainnet-beta.solana.com

# Optional: QuickNode endpoints (recommended for production)
# QUICKNODE_RPC=https://your-quicknode-endpoint.solana-mainnet.quiknode.pro/your-token/
# QUICKNODE_WSS=wss://your-quicknode-endpoint.solana-mainnet.quiknode.pro/your-token/

# Trading Guard - Controls access to trading endpoints (/snipe/intent, /sell/intent)
TRADING_PUBLIC=0                                    # Set to 1 to open trading to everyone
ADMIN_TOKEN=change-me-super-secret                  # Admin override token (CHANGE THIS!)
ALLOWED_WALLETS=YourWallet1,YourWallet2             # Comma-separated base58 wallet pubkeys for closed beta

# Trading Safety Limits
MIN_LIQ_USD=3000                                    # Minimum liquidity in USD
MIN_TOKEN_AGE_MINUTES=20                            # Minimum token age in minutes
MAX_TAX_PCT=10                                      # Maximum buy/sell tax percentage
MIN_RISK_SCORE=40                                   # Minimum XME risk score (0-100, reject below this)

# Platform Fee Configuration
FEE_WALLET=YourSolanaWalletAddressHere              # Platform fee recipient wallet (base58)
FEE_PERCENTAGE=1.0                                  # Platform fee percentage (1.0 = 1%)

# XME Risk Engine
XME_RISK_TTL_MS=20000                               # Risk score cache TTL (20 seconds default)

# Optional: Monitoring & Logging
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_TRACES_SAMPLE_RATE=0.1

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000                          # 1 minute window
RATE_LIMIT_MAX=120                                  # Max requests per window

# Optional: Trade & Wallet Ingest
# TRADE_INGEST=on
# WALLET_INGEST=on
